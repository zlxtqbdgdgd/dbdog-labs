{
  "name": "dbdog-agent-obs",
  "version": "0.7.1",
  "description": "dbdog Agent Observability client hooks for Claude Code（mint → propagate → synthesize → sweep；子代理内部调用可见，span 树三层。脚本本体在 claude-code-hooks/，hooks/hooks.json 经 ${CLAUDE_PLUGIN_ROOT} 引用；另带三个 skill：span-graph（hook span → 假设图,零模型）、evidence-chain（现象+根因+修复 → 反向真取证,五段式）、diag-compare（正反对比 → 六类结论 + dbdog 改进清单,跨单号聚合），见 skills/*/SKILL.md）"
}
